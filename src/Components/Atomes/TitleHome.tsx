import { Image } from '@mantine/core';
import { Box } from '@mantine/core';

export function TitleHome() {
  return (
    <Box pb={30}>
   <h1 style={{textAlign: 'center', fontSize:'2em', fontWeight:'bold'}}>NOS COMPLÉMENTS ALIMENTAIRES LES PLUS POPULAIRES</h1>
  </Box>
  );
}